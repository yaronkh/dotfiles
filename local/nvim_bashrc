export NVIM_BASHRC="executed"
function nvim() {
    (
      VENV_ROOT=~/.config/dotfiles/venv
      export PYTHONPATH=$(python -c "import site; print(':'.join(site.getsitepackages()))"):$PYTHONPATH
      source $VENV_ROOT/bin/activate
      command nvim $@
    )
}

