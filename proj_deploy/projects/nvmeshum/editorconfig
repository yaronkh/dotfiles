 # .editorconfig
    root = true

    [*]
    end_of_line = lf
    charset = utf-8
    trim_trailing_whitespace = false
    insert_final_newline = false

    [{*.{awk,c,dts,dtsi,dtso,h,mk,s,s},kconfig,makefile,makefile.*,h}]
    indent_style = tab
    tab_width = 8

    [*.py]
    indent_style = space
    indent_size = 4
